export type CartItem = {
  id: string
  productId: string
  productSku: string
  productName: string
  productSlug: string
  categorySlug: string
  quantity: number
  unitPrice: number
  totalPrice: number
}
